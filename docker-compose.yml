services:
  messenger:
    build:
      context: .
      dockerfile: messenger/Dockerfile
    ports:
      - "8080:8080"

    env_file:
      - .env

    volumes:
      - ./messenger/app/db/data:/code/messenger/app/db/data
